class Automation:
    ...

# TODO: Import | all from CGPT_AR, schedule, pyperclip
# 
# TODO: INIT | ( internetAccess(requests/chrome/undetected) ...)
# 
# TODO: Variables | internetAccess, loginInfo(loginURL, un, pw),
#                   emailInfo(template, senderFirstName, senderEmail, recipientFirstName, recipientEmail),
#                   runAtStartup(yes or no), 
# TODO: Methods | elementChecker (update to return found element and have optional sendkeys argument - can also
#               do seprately with sendkeys if desired), scheduler (use schedule module + runAtStartup), 
#               emailer(email client, un, pw), login (loginInfo), Browser(internetAccess, URL), runAtStartup, 
#               multi-XPATH/SELECTOR check
#
# TODO: Eventually | add integration for OS automation
# 
# TODO: Create documenation